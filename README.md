# OCR CRM

Веб-приложение для распознавания данных учеников с фотографий и отправки в AMO CRM.

## Возможности

- 📸 Загрузка фото с телефона через веб-интерфейс
- 🤖 OCR распознавание через OpenRouter API (Gemini)
- 💾 Сохранение данных в MongoDB
- 📊 Админ-панель для просмотра заявок
- 📤 Отправка заявок в AMO CRM

## Требования

- Python 3.10+
- MongoDB
- OpenRouter API ключ

## Установка

1. Клонируйте репозиторий:
```bash
cd OCR-crm
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Заполните переменные окружения в `.env`:
```
OPENROUTER_API_KEY=your_key_here
MONGODB_URI=mongodb://localhost:27017
ADMIN_PASSWORD=your_password
...
```

## Запуск

### Быстрый запуск

1. Активируйте виртуальное окружение (если ещё не активировано):
```bash
source venv/bin/activate  # Mac/Linux
# или
venv\Scripts\activate  # Windows
```

2. Запустите сервер:
```bash
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
```

Флаг `--reload` включает автоматическую перезагрузку при изменении кода (удобно для разработки).

### Production (без перезагрузки)

```bash
uvicorn backend.main:app --host 0.0.0.0 --port 8000 --workers 4
```

### После запуска

- Откройте в браузере: **http://localhost:8000**
- Админ-панель: **http://localhost:8000/admin**
- API документация: **http://localhost:8000/docs**

## Использование

### Загрузка фото

1. Откройте http://localhost:8000 в браузере
2. Загрузите фото листка с данными ученика
3. Система автоматически распознает данные

### Админ-панель

1. Откройте http://localhost:8000/admin
2. Введите пароль администратора
3. Просмотрите список заявок
4. Нажмите "Отправить в АМО" для отправки заявок

## API Endpoints

### Публичные

- `POST /api/upload` - загрузка фото для OCR
- `POST /api/upload/manual` - ручной ввод данных

### Админ (требуется авторизация)

- `POST /api/admin/login` - авторизация
- `GET /api/admin/students` - список заявок
- `GET /api/admin/students/{id}` - детали заявки
- `DELETE /api/admin/students/{id}` - удаление заявки
- `PUT /api/admin/students/{id}` - редактирование заявки
- `POST /api/admin/send-to-amo` - отправка в AMO
- `GET /api/admin/stats` - статистика

## Настройка AMO CRM

1. Создайте интеграцию в AMO CRM
2. Получите `client_id` и `client_secret`
3. Авторизуйтесь и получите `access_token` и `refresh_token`
4. Добавьте данные в `.env` файл

## Структура проекта

```
OCR-crm/
├── backend/
│   ├── main.py              # Точка входа
│   ├── config.py            # Конфигурация
│   ├── routes/              # API эндпоинты
│   ├── services/            # Бизнес-логика (OCR, AMO)
│   ├── models/              # Модели данных
│   ├── database/            # MongoDB подключение
│   └── utils/               # Утилиты (авторизация)
├── frontend/
│   ├── admin.html           # Админ-панель
│   ├── admin.js
│   ├── admin.css
│   └── upload.html          # Страница загрузки
├── uploads/                 # Загруженные изображения
├── requirements.txt
├── .env.example
└── README.md
```

## Деплой на Render

Подробная инструкция по развертыванию на Render.com находится в файле [DEPLOY.md](DEPLOY.md)

### Быстрый старт:

1. Загрузите код в Git репозиторий
2. Создайте Web Service на Render.com
3. Установите переменные окружения
4. Деплой произойдёт автоматически

**Важно**: На Render файловая система эфемерная. Для production рекомендуется использовать MongoDB GridFS для хранения изображений.

## Лицензия

MIT

